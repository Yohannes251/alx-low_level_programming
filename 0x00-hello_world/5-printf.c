#include <stdio.h>

/**
 * main - prints a string
 * Return: returns 0 if the function is executed properly
 */
int main(void)
{
	char *str = "with proper grammar, but the outcome is a piece of art,";

	printf("%s", str);
	return (0);
}
