#include <stdio.h>

/**
 * main - Prints a string
 * Return: returns 0 if the function is
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
